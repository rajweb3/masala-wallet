import { StyleSheet, Text, View } from "react-native";
import React from "react";

const TransactScreen = () => {
  return (
    <View>
      <Text>TransactScreen</Text>
    </View>
  );
};

export default TransactScreen;

const styles = StyleSheet.create({});
